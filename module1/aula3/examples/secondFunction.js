function conditionalStructure(){
    name = prompt("Enter name")
    age = prompt("Enter age");

    if (age >= 18) {
        alert(name + " you is Bigger in Age with " + age);
    } else {
        alert(name + " you is Smaller in Age with" + age);
    }
}